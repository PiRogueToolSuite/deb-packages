files:
  - src: reconfigure-pirogue-flow-inspector
    dst: /usr/local/sbin/
    variables:
      - name: ISOLATED_NETWORK_IFACE
    actions:
      - chmod +x /usr/local/sbin/reconfigure-pirogue-flow-inspector
      - /usr/local/sbin/reconfigure-pirogue-flow-inspector
